export { default as QuestionIcon } from './QuestionIcon';
export { default as SendIcon } from './SendIcon';
// Pending icon for the recommendations pod (AT-173): the AI-generated marker for refinement
// chips. It has no consumer yet — don't reach for it until that pod lands.
export { default as SparklesIcon } from './SparklesIcon';
