name: React compat matrix
on:
  pull_request:
    branches:
      - "**"
  workflow_call:
concurrency:
  group: react-compat-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true
jobs:
  compat:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        react-major: ["16", "17", "18", "19"]
    steps:
      - name: Check out code
        uses: actions/checkout@v4
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20.x"
      - name: Install library deps
        run: npm ci
      - name: Build library
        run: npm run compile
      - name: Pack library
        shell: bash
        run: |
          npm pack --pack-destination .
          tarballs=(constructor-io-constructorio-ui-pia-*.tgz)
          if [ ${#tarballs[@]} -ne 1 ]; then
            echo "Expected exactly 1 tarball, found ${#tarballs[@]}: ${tarballs[*]}" >&2
            exit 1
          fi
          mv "${tarballs[0]}" constructorio-ui-pia.tgz
      - name: Install fixture deps
        working-directory: test/react-compat/${{ matrix.react-major }}
        # npm install (not ci) because lock files are gitignored — the tarball
        # is freshly packed each run so its integrity hash always differs.
        run: npm install --no-audit --no-fund
      - name: Typecheck fixture
        working-directory: test/react-compat/${{ matrix.react-major }}
        run: npm run typecheck
      - name: Webpack build
        working-directory: test/react-compat/${{ matrix.react-major }}
        run: npm run build
      - name: Run fixture tests
        working-directory: test/react-compat/${{ matrix.react-major }}
        run: npm test
